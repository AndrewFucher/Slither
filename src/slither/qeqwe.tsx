/*import * as React from 'react';
import * as ReactDOM from 'react-dom';
import slither_body_img_1 from 'src/images/slither_body_img_1.svg';
import helpMe from './Slither';

/* constructor() {
        super({});
        this.bodysegments = 3;
        this.mus = Array(this.bodysegments);
}

let mus: any[];

class Game {

    public bodysegments: number = 8;

    public run() {

        mus = new Array(this.bodysegments);

        for (let i: number = 0; i < this.bodysegments; i++) {
            mus[i] = <img src={slither_body_img_1} id={String(i)} key={i}/>;
         }

        ReactDOM.render(
            <div />,
            document.getElementById('root') as HTMLElement
        );
        

        setTimeout(helpMe, 1000, this.bodysegments, mus);
        // this.mus.push(<img src={slither_body_img_1} id={String(2)} key={2}/>)

    }
}

// tslint:disable-next-line:interface-name


// tslint:disable-next-line:max-classes-per-file


export default Game;*/